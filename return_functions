def is_leap_year(year):
    if int(year) % 400 == 0:
        return True
    elif int(year) % 100 == 0:
        return False
    elif int(year) % 4 == 0:
        return True
    else:
        return False

#the above code checks for leap years all in one function
